#include "TalkWindows.hpp"

#include "SimplePolygonPanel.hpp"
#include "TextPanel.hpp"

namespace plnt {
	/*namespace gui_objects {

		bool TalkWindow::InitializeProc()
		{
			back_ground_panel_ = AddComponent<gui_components::SimplePolygonPanel>();
			back_ground_panel_->SetVertexes({ Vector2Dd(0.0,0.0),Vector2Dd(1.0,0.0), Vector2Dd(1.0,1.0), Vector2Dd(0.0,1.0), });
			back_ground_panel_->fill_color(Color::White());
			back_ground_panel_->outline_color(Color::Black());
			text_panel_ = back_ground_panel_->AddChild<gui_components::TextPanel>();
			return true;
		}

	}*/
}